import React from "react"

const About = React.createClass({
    render(){
        return(
            <div>
                <button>About Us</button>
            </div>
        );
    }

});

export default About;