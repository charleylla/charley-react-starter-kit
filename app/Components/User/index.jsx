import React from "react"

const User = React.createClass({
    render(){
        return(
            <div>
                <button>User</button>
            </div>
        );
    }
})

export default User;